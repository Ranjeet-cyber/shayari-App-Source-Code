<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example42041.fathersdayquotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/alert_dark_frame"
        android:supportsRtl="true"
        android:theme="@style/Theme.Swipecardview">
        <activity
            android:name=".desc2"
            android:exported="true" />
        <activity android:name=".desh"></activity>
        <activity android:name=".favorite">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".desc" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>